#include <iostream>
#include <map>

using namespace std;

int main()
{
    string s1, s2;
    while(cin>>s1>>s2 && s1 != "")
    {
        map<string, string> dic;
        map[s2] = s1
    }
    return 0;
}
