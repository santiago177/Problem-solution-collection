#include <iostream>
#include <cmath>
#include <queue>

using namespace std;

int main()
{
    string c, target;
    while(cin>>c>>target)
    {
        bool state = true;
        if(state)
        {
            priority_queue<int> front;
            priority_queue<int> back;
            for(int a = 0; a < 3; a++)
            {

            }
        }
        else
        {
            priority_queue<int> front;
            priority_queue<int> back;
            for(int a = 0; a < 3; a++)
            {

            }
        }
    }
    return 0;
}
