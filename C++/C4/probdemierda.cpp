#include <iostream>

using namespace std;

int main()
{
    int n;
    scanf("%d", &n);
    for(int z = 0; z < n; z++)
    {
	int x1, y1, u1, v1, x2, y2, u2, v2;
	scanf("%d %d %d %d %d %d %d %d", &x1, &y1, &u1, &v1, &x2, &y2, &u2, &v2);
	
    }
    return 0;
}
